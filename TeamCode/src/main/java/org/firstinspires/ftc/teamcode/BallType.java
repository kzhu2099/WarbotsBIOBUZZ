package org.firstinspires.ftc.teamcode;

/**
 * The two kinds of scoreable game element (BIOBUZZ terminology). Pollen is
 * the neutral, yellow element usable by either alliance. Nectar is
 * alliance-colored (red/blue) and exclusive to that alliance.
 *
 * There is no "jar" type and no "tipper" type. Only these two.
 */
public enum BallType {
    POLLEN,
    NECTAR
}
